<div class="absolute inset-0 w-full h-full overflow-hidden pointer-events-none bg-black">
  <video 
    id="hero-video"
    autoplay 
    loop 
    muted 
    playsinline 
    preload="auto"
    poster={`${import.meta.env.BASE_URL}video/poster.jpg`}
    class="absolute inset-0 w-full h-full object-cover z-0 opacity-0 transition-opacity duration-1000"
  >
    <source src={`${import.meta.env.BASE_URL}video/bg-boda.mp4`} type="video/mp4" />
  </video>

  <div 
    class="absolute inset-0 w-full h-full bg-cover bg-center z-[-1]"
    style={`background-image: url('${import.meta.env.BASE_URL}video/poster.jpg');`}
  >
  </div>

  <div class="absolute inset-0 bg-black/40 z-10"></div>
</div>

<script>
  const video = document.getElementById('hero-video') as HTMLVideoElement;

  if (video) {
    video.addEventListener('canplay', () => {
      video.classList.remove('opacity-0');
    });

    video.muted = true;
    video.play().catch(() => {
      console.log("Autoplay bloqueado. Se muestra poster.");
    });
  }
</script>